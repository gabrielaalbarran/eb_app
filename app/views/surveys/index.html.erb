<p id="notice"><%= notice %></p>

<h1>Encuestas</h1>

<table class="table">
  <thead class="thead-dark">
    <tr>
      <th>Nombre</th>
      <th>Tipo</th>
      <th>Descripción</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @surveys.each do |survey| %>
    <tr>
      <td><%= survey.name %></td>
      <td><%= survey.kind %></td>
      <td><%= survey.description %></td>
      <td><%= link_to survey do %>
        <i class="fas fa-eye fa-2x" style="color:rebeccapurple"></i>
        <% end %>
      </td>
      <td><%= link_to edit_survey_path(survey) do %>
        <i class="fas fa-tools fa-2x" style="color:rebeccapurple"></i>
        <% end %>
      </td>
      <td><%= link_to survey, method: :delete, data: { confirm: 'Are you sure?' } do %>
<i class="far fa-trash-alt fa-2x" style="color:rebeccapurple"></i>
<% end %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Survey', new_survey_path %>
